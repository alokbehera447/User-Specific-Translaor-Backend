# Core Python Dependencies for Translator App
# Complete pip-based installation (no conda required)
# Install with: pip install -r requirements-pip.txt

# ============================================================================
# PYTHON VERSION REQUIREMENT
# ============================================================================
# Required: Python 3.10 or 3.11
# Recommended: Python 3.10 (best compatibility)
# NOT COMPATIBLE: Python 3.12+ (NumPy, PyTorch issues)
# NOT RECOMMENDED: Python 3.8-3.9 (too old)
#
# Check your version: python --version
# ============================================================================

# ============================================================================
# PyTorch Installation Instructions - INSTALL FIRST!
# ============================================================================
# Install PyTorch BEFORE running pip install -r requirements-pip.txt
#
# For GPU (CUDA 11.8):
# pip3 install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 --index-url https://download.pytorch.org/whl/cu118
#
# For GPU (CUDA 12.1):
# pip3 install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 --index-url https://download.pytorch.org/whl/cu121
#
# For CPU Only:
# pip3 install torch==2.1.0+cpu torchvision==0.16.0+cpu torchaudio==2.1.0+cpu --index-url https://download.pytorch.org/whl/cpu
# ============================================================================

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
starlette==0.27.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2  # Fixed version for Python 3.11+ compatibility

# AI/ML Models
openai-whisper==20231117
transformers==4.35.2
sentencepiece==0.1.99
accelerate==0.25.0

# TTS (Text-to-Speech) - Simplified to F5-TTS only
gTTS==2.5.4  # Fallback TTS

# F5-TTS (Primary voice cloning engine)
f5-tts==0.0.2
cached-path==1.6.2
jieba==0.42.1
pypinyin==0.51.0

# Audio Processing
pydub==0.25.1
soundfile==0.12.1
librosa==0.10.1
audioread==3.0.1
SpeechRecognition==3.10.1  # Required for F5-TTS audio transcription
resemble-enhance  # AI-powered audio denoising and enhancement

# Japanese Text Processing (for multilingual TTS support)
cutlet==0.4.0
mecab-python3==1.0.6
unidic-lite==1.0.8
unidic==1.1.0

# Additional ML Dependencies
torch-audiomentations==0.12.0
torch_pitch_shift==1.2.5
pyannote.audio==3.3.2
torchmetrics==1.7.1

# Language Detection
langid==1.1.6

# Deep Learning Optimization (optional, may have installation issues)
# Uncomment if you need it and have proper CUDA setup
# deepspeed==0.12.6

# Type Checking & Validation
pydantic==1.10.13
typing-extensions>=4.8.0

# Utilities
click==8.1.8
tqdm==4.66.1
requests==2.32.3
numpy==1.24.3
scipy==1.11.4

# Additional dependencies for TTS
inflect==7.0.0
gruut[de,es,fr]==2.2.3
phonemizer==3.2.1

# CORS and middleware
python-dotenv==1.0.0
